# nowplaying-sdl configuration file
# This file configures the SDL2-based now playing display

[nowplaying]
# Display settings
display = 0
rotation = 90

# API settings
api_url = http://localhost:1080/api

# Orientation (use one or neither for auto-detect)
# portrait = false
# landscape = false

# Button appearance
bw_buttons = false
no_control = false
minimal_buttons = false

# Initial state
liked = false

# Demo mode (uses sample data instead of AudioControl API)
demo = false
